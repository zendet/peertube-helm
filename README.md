# Helm chart for PeerTube

```sh
helm repo add peertube https://zendet.github.io/peertube-helm/
helm install peertube --namespace peertube peertube/peertube --create-namespace
```
**The documentation can be found at: https://docs.joinpeertube.org**